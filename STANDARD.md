# STANDARD.md

> 权限：只读｜规则：草拟+审批后更新；只追加不删旧｜作用：代码规范、命名约定、性能约束。每次编辑代码前必须查阅，确保产出符合项目标准。
---

## 使用说明

- 每次编辑代码前必须查阅本文件。
- 只读：规范调整需草拟+审批后更新；变更在 CHANGELOG.md 留痕并通知相关维护者。

## 待填入基线


- 代码风格：（如 ESLint / Prettier / Black 等配置）
- 命名约定：（变量 / 函数 / 类 / 文件 / 常量）
- 注释规范：（文件头 / 函数 / 复杂逻辑）
- 提交规范：（如 Conventional Commits）
- 性能约束：（响应时间 / 内存 / 并发等阈值）
- 测试规范：（覆盖率要求 / 命名 / 目录）
- Git工作流：（分支策略 main/develop/feature/release/hotfix / PR流程 / Code Review规则 / 合并门槛）
- 错误处理规范：（异常分类 / 错误码段分配 / 捕获策略 / 全局异常处理）
- 日志规范：（日志级别使用场景 / 格式 / 脱敏规则 / 采集与存储）
- 依赖管理规范：（版本锁定策略 / 升级流程 / 禁止依赖清单 / 锁文件管理）
- 安全编码规范：（输入校验 / SQL注入防范 / XSS防范 / 敏感数据处理，详见 SECURITY.md）
- **视觉色调规范**：见下文「视觉色调规范」章节，新增页面/组件前必查。

## 规范契约

（具体规范条目，由agent草拟、Owner审批后按条目格式追加）

---

## 视觉色调规范

**目的：** 统一全站视觉语言，防止颜色混乱。**新增页面/组件前必查，禁止使用本规范外的颜色。**

### 1. 色彩使用原则

1. **色板封闭**：仅允许使用下文「核心色板」中登记的颜色，禁止私自 `#xxxxxx` 硬编码。
2. **语义优先**：颜色必须通过语义化变量引用（如 `color-primary`、`color-danger`），不得直接使用色值。
3. **状态固定**：成功/警告/错误/信息四态颜色全局唯一，不得在不同模块使用不同色值。
4. **深浅配对**：每个语义色必须定义明确的深浅变体（hover/active/disabled），避免临时调透明度。

### 2. 核心色板（待项目接入时填入）


#### 2.1 品牌色（Primary）

| 语义变量 | 色值 | 用途 |
|----------|------|------|
| `color-primary` | 待填入 | 主按钮、重点强调、品牌识别 |
| `color-primary-hover` | 待填入 | 主按钮悬停 |
| `color-primary-active` | 待填入 | 主按钮按下 |
| `color-primary-disabled` | 待填入 | 主按钮禁用 |

#### 2.2 功能状态色（Semantic）

| 语义变量 | 色值 | 用途 |
|----------|------|------|
| `color-success` | 待填入 | 成功提示、完成状态 |
| `color-warning` | 待填入 | 警告提示、待处理状态 |
| `color-error` | 待填入 | 错误提示、危险操作 |
| `color-info` | 待填入 | 中性信息提示 |

#### 2.3 中性色（Neutral）

| 语义变量 | 色值 | 用途 |
|----------|------|------|
| `color-text-primary` | 待填入 | 主要文本 |
| `color-text-secondary` | 待填入 | 次要文本 |
| `color-text-disabled` | 待填入 | 禁用文本 |
| `color-border` | 待填入 | 分割线、边框 |
| `color-bg-base` | 待填入 | 页面背景 |
| `color-bg-card` | 待填入 | 卡片/容器背景 |

#### 2.4 扩展色（可选，按需登记）

| 语义变量 | 色值 | 用途 |
|----------|------|------|
| `color-link` | 待填入 | 链接文本 |
| `color-mask` | 待填入 | 遮罩层 |

### 3. 暗黑模式（如适用）

若项目支持暗黑模式，必须为上述每个语义变量定义对应的 dark 变体，登记在此：

| 语义变量 | Light 色值 | Dark 色值 |
|----------|-----------|-----------|
| `color-primary` | 待填入 | 待填入 |
| `color-bg-base` | 待填入 | 待填入 |
| ... | ... | ... |

### 4. 颜色使用红线

1. ❌ **禁止硬编码色值**：代码中不得出现 `#1890ff`、`rgb(24,144,255)` 等字面量，必须走语义变量。
2. ❌ **禁止跨语义混用**：不得用 `color-primary` 表示"成功"，不得用 `color-error` 表示"重点"。
3. ❌ **禁止新增未登记颜色**：如需新颜色，必须先在本规范追加登记（走草拟+审批流程），再在代码中使用。
4. ❌ **禁止使用纯黑纯白**：文本/背景应使用登记的中性色，而非 `#000000` / `#FFFFFF`。

### 5. 新增颜色的流程

1. agent 在 DECISION.md 记录"新增颜色"决策（含理由、预期用途）。
2. agent 在本章节草拟新颜色行（语义变量名 + 色值 + 用途）。
3. Owner 审批通过后，颜色方可使用。
4. 变更同步登记到本文件「变更历史」。

---

> 变更历史：见 .git + CHANGELOG.md
