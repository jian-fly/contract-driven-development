# DATA.md

> 权限：agent可读写｜规则：直接更新；只追加不删旧（旧条目标记"被更新"）｜作用：数据模型、表结构、ER关系、数据字典、迁移策略。涉及数据结构变更时查阅，数据模型变更后更新。
---

## 使用说明

- 涉及数据表新增、字段变更、索引调整、迁移脚本编写时，必须先查阅本文件。
- agent可直接读写本文件，但每次数据结构变更必须在 CHANGELOG.md 留痕。
- 数据结构变更若影响接口响应，需同步更新 API.md。
- 旧表/字段废弃时，不得删除条目，标记为"被更新"并注明替代方案与迁移路径。
- 敏感字段（如密码、身份证号）的存储与脱敏规范详见 SECURITY.md。
- **分片触发**：当本文件数据表数超过 50 张时，按业务域拆分到各模块契约（modules/MODULE_XXX.md）的"数据模型"章节，本文件降级为数据索引（仅保留 TBL-ID 和归属模块）。分片后修改数据表只需读对应模块契约。

## 待填入基线


- 数据库类型：（MySQL / PostgreSQL / MongoDB 等）
- ORM/数据访问层：（如 MyBatis / GORM / Prisma 等）
- 表命名规范：（前缀 / 复数单数 / 分库分表规则）
- 主键策略：（自增 / UUID / 雪花ID）
- 索引规范：（命名 / 必建索引 / 联合索引上限）
- 字段命名规范：（蛇形 / 驼峰 / 时间字段格式）
- 软删除策略：（是否启用 / 字段名）
- 迁移工具：（如 Flyway / Liquibase / Prisma Migrate）
- 数据备份与恢复策略：
- 读写分离/分库分表方案：

## 数据模型清单

> 新增数据表时按以下格式追加：

```
### [表名] - [中文说明]
- 用途：
- 字段清单：
  | 字段名 | 类型 | 可空 | 默认值 | 说明 |
  |--------|------|------|--------|------|
- 主键：
- 索引：
- 关联表：（外键关系）
- 软删除：（是/否）
- 敏感字段：（如有，注明脱敏方式）
```

## 数据字典

> 枚举值、状态码、常量定义：

| 字典名 | 键 | 值 | 说明 |
|--------|----|----|------|
| （待填入） | | | |

---

> 变更历史：见 .git + CHANGELOG.md
