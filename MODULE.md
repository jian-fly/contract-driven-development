# MODULE.md

> 权限：agent可读写｜规则：直接更新；只追加不删旧（旧条目标记"被更新"）｜作用：模块目录索引。登记所有模块的ID、路径、职责摘要、模块契约路径。定位模块时查阅，新增模块时更新。
---

## 使用说明

- 本文件是**模块目录索引**，只记录模块的摘要信息（一句话职责 + 路径 + 模块契约位置）。
- 每个模块的详细信息（文件清单、对外接口、数据模型、内部规范）存放在 `modules/MODULE_XXX.md`。
- 新增模块时：先在本文件登记索引行，再在 `modules/` 下创建该模块的契约文件（从 `modules/_TEMPLATE.md` 复制）。
- 模块删除时，不得删除条目，标记为"被更新"并注明废弃原因。
- **当模块内文件超过 10 个时，必须创建独立的模块契约文件**（modules/MODULE_XXX.md）。
- 模块对外暴露的接口详见该模块的模块契约或全局 API.md（未分片时）。

## 模块定义

> **模块 = 一个独立目录，包含一组高内聚文件，共同完成一个明确的业务职责，对外暴露统一接口。**

一个合格模块必须能回答：
1. 我负责什么 / 不负责什么（职责边界）
2. 我包含哪些文件，每个文件负责什么（文件级职责映射）
3. 我对外暴露什么（接口清单）
4. 我依赖谁（依赖关系）
5. 我的内部规范（该模块特有约定）

以上信息登记在该模块的契约文件 `modules/MODULE_XXX.md` 中。

## 模块目录索引

| 模块ID | 模块名 | 路径 | 职责摘要 | 模块契约 | 负责人 | 稳定性 | 状态 |
|--------|--------|------|----------|----------|--------|--------|------|
| （待填入） | | | | modules/MODULE_XXX.md | | | |

## 分片触发规则

当全局契约条目数超过阈值时，自动拆分到模块契约：

| 契约 | 阈值 | 拆分方式 | 拆分后原文件 |
|------|------|----------|-------------|
| API.md | 50个接口 | 按业务域拆分到各模块契约的"对外接口"章节 | 降级为接口索引（仅保留API-ID和归属模块） |
| DATA.md | 50张表 | 按业务域拆分到各模块契约的"数据模型"章节 | 降级为数据索引（仅保留TBL-ID和归属模块） |

拆分后，agent 修改接口/数据表时，直接读对应模块契约即可，无需读全局文件。

## 全局文件映射

> 记录跨模块的关键文件归属，便于快速定位：

| 文件路径 | 所属模块 | 说明 |
|----------|----------|------|
| （待填入） | | |

---

> 变更历史：见 .git + CHANGELOG.md
