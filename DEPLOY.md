# DEPLOY.md

> 权限：agent草拟 + Owner审批｜规则：草拟+审批后更新；只追加不删旧｜作用：环境划分、CI/CD流程、发布与回滚策略、环境变量管理。涉及部署/环境变更时查阅。
---

## 使用说明

- 涉及环境配置变更、发布流程调整、新增环境变量时，必须先查阅本文件。
- 本文件为 agent草拟+Owner审批 权限，部署配置敏感，agent不得直接修改，需草拟后提交Owner审批。
- 环境变量中的密钥/凭证不得明文写入本文件，仅记录变量名与用途，实际值存于密钥管理系统（详见 SECURITY.md）。
- 部署架构变更若涉及架构调整，需同步更新 PROJECT.md。

## 待填入基线


- 环境清单：（dev / test / staging / prod 各自用途与访问方式）
- CI/CD工具：（如 GitHub Actions / GitLab CI / Jenkins）
- 容器化方案：（Docker / K8s / Serverless）
- 镜像仓库：
- 发布流程：（构建→测试→部署的完整步骤）
- 发布策略：（蓝绿 / 滚动 / 灰度）
- 回滚策略：（触发条件 / 回滚步骤 / 数据回滚）
- 环境变量管理：（存放位置 / 命名规范 / 轮换策略）
- 域名与证书管理：
- 监控与告警：（监控工具 / 告警阈值 / 通知渠道）

## 环境配置

> 各环境差异配置，密钥仅记变量名不记值：

| 环境变量名 | 用途 | dev | test | staging | prod |
|------------|------|-----|------|---------|------|
| （待填入） | | | | | |

## 发布记录

> 每次发布追加记录，回滚不删除原记录：

```
### [版本号] - [发布日期]
- 发布环境：
- 变更内容：
- 关联变更批次号：
- 发布结果：（成功/回滚）
- 回滚原因：（如适用）
```

---

> 变更历史：见 .git + CHANGELOG.md
