# INDEX.md

> 权限：只读｜规则：草拟+审批后更新；只追加不删旧｜作用：契约文件总导航。记录所有契约文件的功能、权限、查看时机。当不确定应查阅哪份契约时，先查看本文件。
---

## 契约系统元信息

- **契约系统版本：** v2.4.0
- **变更历史：** 记录在 .git；语义理由见 CHANGELOG.md
- **Owner（审批者）：** （项目接入时填入，拥有所有只读/人工审批契约的最终审批权）
- **生效日期：** 2026-08-03
- **采用级别：** （项目接入时选择：Lite / Standard / Full）
- **全局契约数：** 10
- **模块契约目录：** modules/

## 使用说明

- 唯一入口：不确定查哪份契约时先读本文件；新增/变更契约文件时同步更新本索引。
- 只读：需草拟变更提交 Owner 审批；变更留痕见 CHANGELOG.md。

## 契约采用级别

> 契约系统支持三级渐进式接入，项目可随规模成长平滑升级，避免一开始就过度设计。**项目类型不再仅以代码行数或模块数划分，而是引入“项目复杂度评分 (PCS)”进行多维评估。**

### 项目复杂度评分 (Project Complexity Score, PCS)

PCS 是基于契约系统内已有数据构建的复合评分，用于科学评估项目管理复杂度。

**计算公式：**
`PCS = (模块数 × 30%) + (接口数 × 25%) + (数据表数 × 20%) + (决策数 × 15%) + (环境数 × 10%)`

| 维度 | 权重 | 数据来源 | 说明 |
|------|------|----------|------|
| 模块数 | 30% | MODULE.md | 核心权重，代表代码组织的分散程度 |
| 接口数 | 25% | API.md | 代表系统对外暴露的复杂度 |
| 数据表数 | 20% | DATA.md | 代表数据模型的复杂度 |
| 决策数 | 15% | DECISION.md | 代表技术选型与历史包袱的复杂度 |
| 环境数 | 10% | DEPLOY.md | 代表部署与运维的复杂度 |

### PCS 分数区间与采用级别映射

| PCS 分数区间 | 采用级别 | 适用场景 | 包含契约 | 初始化步骤 |
|-------------|----------|----------|----------|------------|
| **0 - 15 分** | **Lite** | 低复杂度项目（POC、个人项目、简单工具） | INDEX / PROJECT / STANDARD / API / MODULE | 3步 |
| **16 - 40 分** | **Standard** | 中复杂度项目（核心业务、多人协作、中型系统） | Lite + DECISION / DATA / TEST | 7步 |
| **41+ 分** | **Full** | 高复杂度项目（大型平台、微服务架构、合规要求） | Standard + SECURITY / DEPLOY + modules/分片 | 11步 |

### 升级触发条件（满足任一即升级）

1.  **PCS 分数跨越阈值**（15分/40分）。
2.  **定性硬触发条件**（不受分数限制）：
    *   **强制升级至 Standard**：
        *   出现多人协作或多人审批需求。
        *   项目涉及核心业务逻辑（如支付、鉴权）。
    *   **强制升级至 Full**：
        *   出现部署流水线、合规要求或数据迁移。
        *   项目需要 7x24 小时高可用性。
3.  Owner 主动要求升级。

**降级规则：** 原则上不允许降级。如需降级，必须在 DECISION.md 记录理由，并清理不再使用的契约文件。

---

## 契约文件索引

### 全局契约（跨模块共享）

| 文件 | 采用级别 | 权限 | 作用 | 查看时机 | 更新时机 |
|------|----------|------|------|----------|----------|
| INDEX.md | 全部 | 只读 | 契约总导航 | 不确定查哪份契约时 | 新增/变更契约文件时 |
| PROJECT.md | 全部 | 人工审批 | 架构设计、模块划分、技术契约 | 涉及架构调整时 | 架构/模块/技术方案变更时 |
| STANDARD.md | 全部 | 只读 | 代码规范、命名约定、性能约束 | 每次编辑代码前 | 规范/约定调整时 |
| API.md | 全部 | agent可读写 | 接口定义（超50个后降级为索引） | 涉及接口变更时 | 修改接口后 |
| MODULE.md | 全部 | agent可读写 | 模块目录索引 | 定位模块时 | 新增/删除模块时 |
| DECISION.md | Standard+ | agent可读写 | 技术决策记录 | 做技术选择前 | 做决策后 |
| DATA.md | Standard+ | agent可读写 | 数据模型（超50张后降级为索引） | 涉及数据结构变更时 | 数据模型变更后 |
| TEST.md | Standard+ | agent可读写 | 测试策略、覆盖率门槛、Mock规范、E2E | 编写测试前 | 测试策略变更时 |
| SECURITY.md | Full | 只读 | 鉴权授权、密钥管理、审计日志、合规红线 | 涉及鉴权/敏感数据时 | 安全策略变更时 |
| DEPLOY.md | Full | agent草拟+Owner审批 | 环境划分、CI/CD、发布回滚、环境变量 | 涉及部署/环境变更时 | 部署架构变更时 |

### 模块契约（按模块拆分，存放于 modules/）

| 文件 | 采用级别 | 权限 | 作用 | 查看时机 | 更新时机 |
|------|----------|------|------|----------|----------|
| modules/_TEMPLATE.md | Full | 只读 | 模块契约模板 | 新建模块契约时 | 模板调整时 |
| modules/MODULE_XXX.md | Full | agent可读写 | 单个模块的完整契约（职责/文件/接口/数据/依赖/规范） | 修改该模块功能时 | 模块变更时 |

> 每个模块一个 `MODULE_XXX.md`，从 `_TEMPLATE.md` 复制创建。当模块文件超过10个时必须创建。

## 契约间依赖关系

```
SECURITY.md ──(最高优先级，冲突时以此为准)──> 所有契约
PROJECT.md ──(架构约束)──> MODULE.md ──(模块目录)──> modules/MODULE_XXX.md
modules/MODULE_XXX.md ──(模块对外接口)──> API.md（未分片）/ 模块内部（已分片）
modules/MODULE_XXX.md ──(模块数据模型)──> DATA.md（未分片）/ 模块内部（已分片）
MODULE.md ──(模块测试策略)──> TEST.md
DEPLOY.md ──(部署架构)──> PROJECT.md
任何决策 ──(记录)──> DECISION.md
```

## 待填入基线


- 项目名称：
- 项目简述：
- 契约文件存放路径：
- Owner身份标识：（用户名/工号）
- 业务域划分：（用于后续模块契约的分片）
- 其他项目特定的契约文件（如有）：

---

## 系统性机制

### 1. 变更批次号

跨契约变更时使用统一批次号串联，便于追溯一次完整变更涉及的所有契约。

- 格式：`BATCH-序号-类型`，如 `BATCH-001-ARCH`（架构变更）、`BATCH-002-API`（接口变更）、`BATCH-003-SEC`（安全变更）
- 用法：在 CHANGELOG.md 的变更条目中标注关联批次号
- 登记：批次号与变更摘要记录在 CHANGELOG.md

### 2. 条目级ID

每个接口/模块/数据表/决策条目带唯一ID，可单独追踪生命周期。

- 接口ID：`API-序号`（如 `API-001`），登记于 API.md 或模块契约
- 模块ID：`MOD-序号`（如 `MOD-001`），登记于 MODULE.md 和模块契约
- 数据表ID：`TBL-序号`（如 `TBL-001`），登记于 DATA.md 或模块契约
- 决策ID：`ADR-序号`（如 `ADR-001`），登记于 DECISION.md

### 3. 冲突优先级

当契约间出现矛盾时，按以下优先级裁定（高 → 低）：

```
SECURITY.md > PROJECT.md > STANDARD.md > 模块契约(MODULE_XXX.md) > DATA.md > API.md > MODULE.md > DEPLOY.md > TEST.md > DECISION.md
```

- 安全契约（SECURITY）为最高优先级，任何与之冲突的条目均以 SECURITY.md 为准
- 模块契约优先于全局 API/DATA（模块内有特化约定时以模块为准）
- 冲突无法自行解决时，在 DECISION.md 提交冲突记录，由 Owner 裁定

### 4. 分片触发与模块契约

当项目规模增长时，契约按以下规则分片，避免单文件膨胀：

| 触发条件 | 动作 | 目的 |
|----------|------|------|
| 模块文件超过10个 | 创建 modules/MODULE_XXX.md | 文件级职责映射，快速定位 |
| API.md 超过50个接口 | 拆分到各模块契约的"对外接口"章节 | 避免全局文件过长 |
| DATA.md 超过50张表 | 拆分到各模块契约的"数据模型"章节 | 避免全局文件过长 |
| 模块契约超过500行 | 按子模块进一步拆分 | 控制单文件读取成本 |

**分片后定位路径优化：**
```
未分片：INDEX → MODULE → API/DATA（3次跨文件，全量读取）
已分片：INDEX → MODULE → modules/MODULE_XXX.md（3次，但只读相关模块，精准高效）
```

### 5. 首次接入Checklist（按采用级别）

项目创建时，根据选定的采用级别，agent按以下顺序填入各契约基线：

#### Lite 模式（3步，小项目）

1. **INDEX.md** —— 填入项目名称、Owner身份、存放路径、采用级别（Lite）
2. **PROJECT.md** —— 填入核心架构基线（技术栈/模块划分/目录结构）
3. **STANDARD.md + API.md + MODULE.md** —— 填入规范基线 + 接口基线 + 模块目录索引（可并行填入）

#### Standard 模式（7步，中型项目）

1. **INDEX.md** —— 填入项目名称、Owner身份、存放路径、采用级别（Standard）
2. **PROJECT.md** —— 填入完整架构基线（技术栈/模块划分/非功能性需求/环境划分/团队角色/架构红线/演进路线）
3. **STANDARD.md** —— 填入规范基线（代码风格/Git工作流/错误处理/日志/依赖管理）
4. **API.md** —— 填入接口基线（接口风格/统一响应/错误码/分页约定/幂等性/版本策略）
5. **MODULE.md** —— 填入模块目录索引
6. **DATA.md + TEST.md** —— 填入数据层基线 + 测试基线（可并行）
7. **DECISION.md** —— 记录项目初始化阶段的核心技术选型决策

#### Full 模式（11步，大型项目）

1. **INDEX.md** —— 填入项目名称、Owner身份、存放路径、采用级别（Full）
2. **PROJECT.md** —— 填入完整架构基线
3. **SECURITY.md** —— 填入安全基线（鉴权/密钥/敏感数据/安全红线）
4. **STANDARD.md** —— 填入规范基线
5. **DATA.md** —— 填入数据层基线
6. **API.md** —— 填入接口基线
7. **MODULE.md** —— 填入模块目录索引
8. **modules/MODULE_XXX.md** —— 为每个核心模块创建模块契约
9. **DEPLOY.md** —— 填入部署基线
10. **TEST.md** —— 填入测试基线
11. **DECISION.md** —— 记录核心技术选型决策

### 6. 新增契约文件流程

当现有契约无法覆盖新需求时：

1. agent在 DECISION.md 提交"新增契约提议"（含理由、预期职责、权限建议）
2. Owner审批通过后，agent草拟新契约文件框架
3. Owner审批框架后，agent在 INDEX.md 草拟新增索引行
4. Owner审批索引更新后，新契约正式生效

### 7. 自检机制（Token优化版）

**目标**：每次操作完成后，Agent 必须根据本次操作的“变更类型”检查是否更新了对应的契约。**核心是“精准映射”，避免全量扫描。**

#### 7.1 变更类型 → 必检契约映射矩阵

Agent 完成操作后，首先识别本次操作的“变更类型”，然后按下表检查对应契约：

| 变更类型 | 必检契约 | 检查内容 |
|----------|----------|----------|
| **接口变更**（新增/修改/废弃接口） | API.md + 关联模块契约 | 接口条目是否已更新？是否需要同步更新模块契约的接口索引？ |
| **数据变更**（新增/修改表/字段） | DATA.md + 关联模块契约 | 数据表条目是否已更新？敏感字段是否同步 SECURITY.md？ |
| **模块结构变更**（新增/删除/移动模块） | MODULE.md + 关联模块契约 | 模块目录索引是否已更新？是否创建/归档了模块契约？ |
| **技术选型/架构变更** | DECISION.md + PROJECT.md | 是否记录了决策？架构图/模块划分是否需要调整？ |
| **鉴权/安全变更**（涉及权限/密钥/敏感数据） | SECURITY.md | 是否更新了敏感数据清单/鉴权规则？ |
| **部署变更**（环境/CI-CD/发布流程） | DEPLOY.md | 是否更新了环境配置/发布记录？ |
| **代码规范变更**（命名/格式/错误处理） | STANDARD.md | 是否更新了规范条目？ |
| **测试策略变更** | TEST.md | 是否更新了测试策略？ |
| **视觉/UI 变更**（新增页面/组件/颜色） | STANDARD.md | 是否使用了未登记颜色？是否需要登记新颜色？ |

#### 7.2 分级检查清单

根据项目采用级别，调整自检范围：

*   **Lite 模式**：仅检查 `API.md` 和 `MODULE.md`。
*   **Standard 模式**：检查 `API.md`, `MODULE.md`, `DATA.md`, `DECISION.md`。
*   **Full 模式**：检查映射矩阵中的所有涉及契约。

#### 7.3 精简自检汇报（必须输出）

Agent 自检后，必须以**表格形式**输出以下三行内容，确保 Owner 一眼看清。同时，在汇报的**最后必须附上 PCS 评分**，用于监控项目复杂度变化。

| 契约文件 | 状态 | 备注 |
|----------|------|------|
| `API.md` | ✅ 已更新 | 新增接口 API-010 |
| `MODULE.md` | ⏭️ 无需更新 | 本次操作未涉及模块结构 |
| `SECURITY.md` | ❌ 遗漏 | **未更新！接口涉及敏感字段，需同步 SECURITY.md** |

*   ✅ **已更新**：契约已同步修改。
*   ⏭️ **无需更新**：本次操作不涉及该契约。
*   ❌ **遗漏**：该更新但未更新。Agent **必须立即补更新**并重新汇报，不能仅报告。

#### 7.4 PCS 评分监控

每次自检汇报时，Agent **必须自动计算并输出当前的 PCS 评分**，格式如下：

---
**📊 项目复杂度评分 (PCS)：**
- 当前分数：**25.5 分** (Standard 模式)
- 明细：
  - 模块数 (×30%): 5 个 → 1.5
  - 接口数 (×25%): 30 个 → 7.5
  - 数据表数 (×20%): 20 张 → 4.0
  - 决策数 (×15%): 5 条 → 0.75
  - 环境数 (×10%): 1 个 → 0.1
- **⚠️ 提示：** PCS 即将跨越 Lite 模式阈值 (15分)，建议评审是否升级为 Standard 模式。
---

**PCS 监控规则：**
- 如果 PCS 分数跨越了当前采用级别的阈值（15分/40分），Agent **必须在汇报中给出显著提示**，建议 Owner 升级。
- 如果触发了"定性硬触发条件"，Agent 必须在 PCS 汇报中明确指出。

---

> 变更历史：见 .git + CHANGELOG.md
